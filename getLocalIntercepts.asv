function [horiz,vert] = getLocalIntercepts(i,j,theta,matrixSize)
    % locates the distances from the bottom left corner 
    horiz = [];
    vert = [];
    [x,y] = matrixIndexToCoord(i,j,matrixSize);
    
    % check bottom horizontal line
    yBottom = y - 0.5;
    x = yBottom/tand(theta);
    if 
end